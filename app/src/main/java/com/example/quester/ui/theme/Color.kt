package com.example.quester.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
//  COLORI FANTASY - TEMA SCURO (Default)
// ============================================================

// Sfondo e Superfici (Scuro)
val FantasyBackground = Color(0xFF0D0B14)        // Nero violaceo profondo
val FantasySurface = Color(0xFF171321)           // Superficie principale
val FantasySurfaceLight = Color(0xFF221B2E)      // Superficie leggermente più chiara
val FantasySurfaceVariant = Color(0xFF2D1F3D)    // Variante superficie

// Oro e Metallici
val FantasyGold = Color(0xFFD4A84F)              // Oro principale
val FantasyGoldLight = Color(0xFFF0CC78)         // Oro chiaro (effetti luce)
val FantasyGoldDark = Color(0xFFB8923A)          // Oro scuro (ombre)

// Viola e Magici
val FantasyPurple = Color(0xFF6B4C9A)            // Viola principale
val FantasyPurpleDark = Color(0xFF2B1D42)        // Viola scuro (profondo)
val FantasyPurpleLight = Color(0xFF8B6BB0)       // Viola chiaro

// Testo
val FantasyText = Color(0xFFF3EBD8)              // Testo principale (avorio)
val FantasyTextSecondary = Color(0xFFC8BDA8)     // Testo secondario (avorio spento)

// Accenti
val FantasyError = Color(0xFFE57373)             // Rosso errore
val FantasySuccess = Color(0xFF81C784)           // Verde successo
val FantasyWarning = Color(0xFFFFD54F)           // Giallo warning
val FantasyInfo = Color(0xFF64B5F6)              // Blu info

// ============================================================
//  COLORI FANTASY - TEMA CHIARO
// ============================================================

// Sfondo e Superfici (Chiaro)
val FantasyLightBackground = Color(0xFFF5F0E8)   // Beige caldo
val FantasyLightSurface = Color(0xFFFFFFFF)      // Bianco puro
val FantasyLightSurfaceLight = Color(0xFFFBF8F2) // Bianco avorio
val FantasyLightSurfaceVariant = Color(0xFFEDE5D8) // Beige chiaro

// Oro e Metallici (Chiaro)
val FantasyLightGold = Color(0xFFC9A84C)         // Oro più caldo
val FantasyLightGoldLight = Color(0xFFE8D5A0)    // Oro chiaro
val FantasyLightGoldDark = Color(0xFFA8883A)     // Oro scuro

// Viola e Magici (Chiaro)
val FantasyLightPurple = Color(0xFF7B5BA8)       // Viola più brillante
val FantasyLightPurpleDark = Color(0xFF4A2D6E)   // Viola profondo
val FantasyLightPurpleLight = Color(0xFFA88BC8)  // Viola chiaro

// Testo (Chiaro)
val FantasyLightText = Color(0xFF1A1410)         // Testo principale (marrone scuro)
val FantasyLightTextSecondary = Color(0xFF5A4F45) // Testo secondario (marrone)

// Accenti (Chiaro)
val FantasyLightError = Color(0xFFD32F2F)        // Rosso
val FantasyLightSuccess = Color(0xFF388E3C)      // Verde
val FantasyLightWarning = Color(0xFFF9A825)      // Giallo
val FantasyLightInfo = Color(0xFF1976D2)         // Blu

// ============================================================
//  COLORI TEMA ARCADE (PIXEL/RETRÒ)
// ============================================================

// Sfondo e Superfici (Scuro)
val ArcadeBackground = Color(0xFF0A0A0F)        // Nero profondo
val ArcadeSurface = Color(0xFF12121A)           // Blu scuro
val ArcadeSurfaceLight = Color(0xFF1A1A2E)      // Blu più chiaro
val ArcadeSurfaceVariant = Color(0xFF222244)    // Variante

// Colori Neon
val ArcadeGreen = Color(0xFF00FF41)             // Verde Matrix
val ArcadePink = Color(0xFFFF00FF)              // Rosa neon
val ArcadeCyan = Color(0xFF00FFFF)              // Ciano neon
val ArcadeYellow = Color(0xFFFFFF00)            // Giallo neon
val ArcadeOrange = Color(0xFFFF6600)            // Arancione neon

// Testo
val ArcadeText = Color(0xFF00FF41)              // Testo verde neon
val ArcadeTextSecondary = Color(0xFF66FF66)     // Testo secondario

// Accenti
val ArcadeError = Color(0xFFFF0055)             // Rosso neon
val ArcadeSuccess = Color(0xFF00FF66)           // Verde successo

// ============================================================
//  COLORI BACHECA FANTASY (LEGNO E PERGAMENA)
// ============================================================

val FantasyBoardWoodDark = Color(0xFF2C1A0E)     // Legno scuro (Sfondo)
val FantasyBoardWood = Color(0xFF4A2E1B)         // Legno medio (Superfici)
val FantasyBoardWoodLight = Color(0xFF6B4226)    // Legno chiaro (Bordi)

val FantasyBoardParchment = Color(0xFFE8D5A0)    // Pergamena (Testo primario)
val FantasyBoardParchmentDark = Color(0xFFC0A060) // Pergamena scura (Testo secondario)

val FantasyBoardRedWax = Color(0xFF9E1B1B)       // Cera lacca rossa (Error/Accenti)
val FantasyBoardInk = Color(0xFF1A1A1A)          // Inchiostro nero
val FantasyBoardGoldLeaf = Color(0xFFD4AF37)     // Foglia d'oro (Accenti principali)